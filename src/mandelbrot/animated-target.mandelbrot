C       = COORD
z       = C
seconds = MILLIS / 1000
exp     = int(30 * sin(seconds))
density = 3 + sin(seconds)
bailout = 4

do
	z -= (z^exp * C - z + C) / (density * z^(exp - 1) * C)
while
	mod(z) <= bailout